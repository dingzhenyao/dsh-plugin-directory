# Local pnpm settings for dsh-plugin-directory.
# Host packages are declared as peers; fail the install loudly when a peer
# range cannot be satisfied instead of resolving silently.
strict-peer-dependencies=true
