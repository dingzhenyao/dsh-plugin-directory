/** Host loader entry for the plugin directory client implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin directory tab. */
export function apply(): void {}
